// src/components/common/DeviseSelector.jsx

import React from 'react';

// Composant DeviseSelector temporaire
const DeviseSelector = () => {
    return (
        <div style={{ color: 'blue', border: '1px solid blue', padding: '5px' }}>
            [Sélecteur de Devise Temporaire]
        </div>
    );
};

// Exportation par défaut (ce que Header.jsx attend)
export default DeviseSelector;
