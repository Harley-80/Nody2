// src/components/common/UserDropdown.jsx

import React from 'react';

// Composant UserDropdown temporaire
const UserDropdown = () => {
    return (
        <div style={{ color: 'orange', border: '1px dashed orange', padding: '5px' }}>
            [Menu Utilisateur Temporaire]
        </div>
    );
};

// Exportation par défaut (ce que Header.jsx attend: import UserDropdown from "...")
export default UserDropdown;
