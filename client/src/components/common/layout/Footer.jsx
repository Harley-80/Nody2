// src/components/common/layout/Footer.jsx

import React from 'react';

// Composant Footer temporaire
const Footer = () => {
    return (
        <footer
            style={{ background: '#333', color: 'white', padding: '20px', textAlign: 'center' }}
        >
            [Pied de page Temporaire]
        </footer>
    );
};

// Exportation par défaut (C'est ce que 'import Footer from ...' attend)
export default Footer;
