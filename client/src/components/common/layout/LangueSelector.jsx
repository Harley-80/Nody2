// src/components/common/layout/LangueSelector.jsx

import React from 'react';

// Composant LangueSelector temporaire
const LangueSelector = () => {
    return (
        <div style={{ color: 'red', border: '1px dotted red', padding: '5px' }}>
            [Sélecteur de Langue Temporaire]
        </div>
    );
};

// Exportation par défaut (C'est ce que 'import LangueSelector from ...' attend)
export default LangueSelector;
